/**
 * @file Button.tsx
 * @description 프로젝트 전반에서 사용되는 공통 버튼 컴포넌트입니다.
 * * @author [하늘]
 * @param {string} variant - 버튼의 스타일 타입 ('primary' | 'outline' | 'danger')
 * @param {string} size - 버튼 크기 ('sm' | 'md' | 'lg')
 * @param {boolean} disabled - 버튼 비활성화 여부
 * @returns {JSX.Element} 지정된 스타일이 적용된 button 태그
 * * @example
 * <Button variant="primary" size="lg" disabled={false}>저장하기</Button>
 */
